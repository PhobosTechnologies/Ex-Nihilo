spawn_seed = range(ord('a'), ord('c')+1)

for _ in spawn_seed:
	# NihiloSplit(mother_ID, spawn_seed, spawn_interactivity_map, SUBSTITUTE_VAR)
	print(chr(_))

print(ord('L'))

t1 = None
print(t1)
if t1 is None:
	print('it is none')
else:
	print('is not none')
